export const START: HTMLElement = document.getElementById(
    'start'
) as HTMLElement;

export const DIFFICULTY: HTMLElement = document.getElementById(
    'difficulty'
) as HTMLElement;

export const PLAYER1: HTMLElement = document.getElementById(
    'player1'
) as HTMLElement;

export const PLAYER2: HTMLElement = document.getElementById(
    'player2'
) as HTMLElement;
